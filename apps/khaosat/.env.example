# CSDL: SQLite cho dev (mặc định). Với Postgres (harness/deploy) xem ghi chú trong README.
DATABASE_URL="file:./dev.db"
# Cổng HTTP của server
PORT=3000
# Khóa quản trị — ĐỔI trước khi triển khai thật
ADMIN_KEY=khaosat-admin
